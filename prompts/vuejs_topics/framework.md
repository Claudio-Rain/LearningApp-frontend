Styling elements using framework capability

Skills
	1. Uses props to send data to the children component
	2. Uses Slots to render the children component

Knowledge
	1. Component interaction specific and approaches
	2. Slots usage and limitation
	3. How to avoid "prop-drilling"

Uses technics to dynamically generate elements
Skills
	1. Uses directives conditional rendering (v-if, v-else)
	2. Uses loops and iterators for elements creation
	3. Uses keys when dealing with the lists

Knowledge
	1. How to use conditional rendering, and what are the main structures
	2. How to dynamically generate a list and available directives
	3. Importance of using keys
	4. Using correct data for keys
  5. What is a directive?


Uses technics to dynamically generate elements
Skills
	1. Uses directives conditional rendering (v-if, v-else)
	2. Uses loops and iterators for elements creation
	3. Uses keys when dealing with the lists

Knowledge
	1. How to use conditional rendering, and what are the main structures
	2. How to dynamically generate a list and available directives
	3. Importance of using keys
	4. Using correct data for keys

Applies technics and uses technology for styling components

Skills
	1. Applies styles with CSS based on the selected template
	2. Applies styles by CSS-in-js solution (eq emotion, styled component)
	
Knowledge
	1. Limitation of templates and CSS usage inside it
	2. Benefit and drawbacks of CSS-in-JS pattern and tools


Displays data on the user interface
Skills
	1. Uses computed properties and watchers based on the use case.
	2. Uses one-way and two-way data binding
	3. Uses built-in directives

Knowledge
	1. Slots usage and applicability
	2. Difference between one-way and two-way binding
	3. Usage and applicability of computed properties and watchers


Handles user interaction with event handlers in framework
  Skills
1. Creates methods for the event handler
2. Uses `v-on:` or `@` syntax to handle events
3.   Uses Event Modifiers

Knowledge
1. Different between events and event handling in JS
2. Specific and Limitation of handling event in Vue
3. Specifics using Refs in Vue
4. What is a concept of event binding


Interacts with API and handles request metadata
Skills
	1. Uses VueX actions to handle data from API
	2. Uses fetch libraries for an API call
	3. Handles Errors when fetching and sending data through API
	
Knowledge
	1. How to use different HTTP clients in Vue.js
	2. What is a CORS mechanism and specifics of working with it
	3. Difference between Graphql and REST


Interacts with lifecycles in components using framework capability
Skills
	1. Uses data object for state management.
	2. Uses lifecycles methods for lifecycle interaction.(`beforeCreate`, `created`, `beforeUpdate`, `updated`, `beforeDestroy`, `destroyed`)

Knowledge
	1. How state (data) creation and update works
	2. Specifics of lifecycle hooks in the Vue
	3. How to manage state and amount of re-rendering



Sets up, builds, tests, and deploys application with CLI
Skills
	1. Executes basic commands that are provided by CRA after the project is generated (testing, building, development build)

Knowledge
	1. General CLI flow overview in Vue
	2. What is a general purpose of CRA and CLIs
	3. How to generate a project with CLI


Uses basic router capability for building navigation
Skills
	1. Defines route components
	2. Defines routes
	3. Uses named routes and components

Knowledge
	1. Vue router installation and basic configuration
	2. The basic structure of Vue Router
	3. What is SPA (single page application) application
	4. How and why to use nested routing in Vue Router
	5. What are the benefits of the named routing


Applies security practices and approaches to protect application
Skills
	1. Uses HTML content
	2. Uses Attribute bindings

Knowledge
	1. Different types of attacks and security vulnerability
	2. Concept of sanitization of data, use cases, and applicability
	3. Build-in Vue.js methods and attributes to prevent attacks

Defines custom event for handling user interaction
Skills
1. Creates methods for custom event
2. Uses event emitter interface to handle the custom event

Knowledge
1. Structures and use cases of creating custom events
2. Concept and strategy of event emitter
3. Implementation of event emitter in Vue
4. What is new Event structure and how to use it


Handles internationalisation in scope of the applications development
Skills
1. Uses Vue i18n for formating
2. Uses Vue i18n for date and time localization
3. Uses Vue i18n to create a fallback

Knowledge
1. Installation and usage of Vue i18 tools
2. Use cases and applicability data and time internationalization


Manages data for developing dynamic and highly scalable applications
Skills
1. Uses VueX getters to obtain information
2. Uses the VueX store to add data
3. Uses VueX Actions and Mutators to update data

Knowledge
1. Difference between flux and observable, benefits and drawbacks of each solution
2. What is a centralize state management configuration and main building blocks
3. Difference between immutability and mutability, benefits and drawbacks of each approach



Uses advanced router capability for building navigation
Skills
	1. Uses navigation guards to cancel or initiate a redirect
	2. Uses `redirect` for redirection
	3. Handles 404 pages
	4. Uses `alias`
	5. Uses vue-router modes

Knowledge
	1. Vue.js router's main approaches to prevent users from a redirection
	2. How to handle missing pages, usage and use case
	3. Use cases and applicability of Aliases
	4. Difference and use cases between Hash mode and HTML5 History Mode
	5. How to handle front end routes in a monolith application


Uses forms for user inputs and interaction
Skills
1. Uses VueX getters to obtain information
2. Uses the VueX store to add data
3. Uses VueX Actions and Mutators to update data

Knowledge
1. Difference between flux and observable, benefits and drawbacks of each solution
2. What is a centralize state management configuration and main building blocks
3. Difference between immutability and mutability, benefits and drawbacks of each approach


Uses framework built-in tools for code reusability
Skills
1. Uses Inheritance
2. Uses mixins
3. Creates custom directives
4. Creates and uses plugins

Knowledge
1. Difference between Inheritance and mixins in Vue.js
2. How to create and use the mixin in Vue.JS
3. Use cases and applicability for Inheritance
4. Difference between OOP and functional approach in Vue.js
5. Use cases for creating custom directives and plugins


Understands the difference between Options API and Composition API
Skills
1. Uses Options API with data(), computed, methods, and lifecycle hooks
2. Uses Composition API with setup(), ref(), reactive(), and computed()
3. Migrates a component from Options API to Composition API

Knowledge
1. Difference between Options API and Composition API and when to use each
2. How Vue 2 and Vue 3 handle component logic organization
3. Benefits of Composition API for code reuse and TypeScript support
4. How lifecycle hooks map between both APIs (e.g. created → onMounted)


Uses framework-related and basic tools for application's debugging
Skills
1. Uses Vue.js DevTools for inspecting application/component state

Knowledge
2. Installation and usage of Vue.js DevTools in browser


